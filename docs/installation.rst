============
Installation
============

At the command line::

    $ easy_install ftp-handler

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ftp-handler
    $ pip install ftp-handler
